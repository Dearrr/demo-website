import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: `'DM Sans', 'sans-serif'`,
    test: {
      fontWeight: 200,
    },
    white: {
      color: "red",
    },
  },
  palette: {
    mainText: "#ffff",
    secondaryText: "#CDCCD2",
  },
});
